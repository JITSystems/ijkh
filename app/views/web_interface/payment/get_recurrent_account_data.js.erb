$('p.account_amount').html('<%= @account.amount %>');
$('input#account_id').attr('value', '<%= @account.id %>');
$('input#amount_total').attr('value', '<%= @account.amount %>');
var tax_total = Math.round((<%= @account.amount %>*0.03) * 100) / 100;
 $('#tax_total_value').text(tax_total);
$(".end_button").removeAttr("disabled");
$("input#amount_total").removeAttr("disabled");
$('.recipe_is_paid').slideDown();
popUpRender("<%= @message %>");